datasource Database {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator Client {
  provider        = "prisma-client-js"
  previewFeatures = ["mongodb"]
  output          = "./source"
}

model Images {
  hash          String @unique
  extension     String
  originalFile  String @unique
  optimizedFile String @unique
}
